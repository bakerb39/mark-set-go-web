Explorer Designer v2.2.1 — latest user JSON + no center wall + anchored music player

Upload/extract at the repository root so the public/ paths replace the existing files.

What this package does:
- Uses mark-set-go-explorer-design-v2 (4).json as the UI baseline.
- Adds an "Actual reading page color" picker under Reading page.
- Removes the obsolete tan/green center filler behind the Reader shell.
- Keeps the actual music player anchored beneath the top-right music button after a selection starts playing.
- Keeps Explorer/Antique/Custom theme controls.
- No Reader engine, reading mode, timing, pagination, or MutationObserver changes.
