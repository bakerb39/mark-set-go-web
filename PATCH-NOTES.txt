Reader Workspace layout repair
- Built from the public/ files in the latest uploaded full project ZIP.
- Fixes the workspace shell grid so Header, Tabs, and Active Panel have three explicit rows.
- Prevents the tab bar from stretching vertically.
- Forces tabs to stay horizontal and scroll horizontally if space becomes too narrow.
- Enforces a 520px minimum desktop workspace width, including restored splitter widths.
- Updates app.js and reader-workspace-shell.css cache tokens.
- Reader engine modules were not changed.
