Mark, Set, Go! — Double-click-only Reader playback v2

Built from:
- app(5).js
- index(6).html

Fixes:
- Removed Spacebar playback.
- Removed blank-space playback from BOTH existing click paths:
  1. the early Reader pointerup/stable-click handler
  2. the later readerFrame click handler
- This is the key fix for scrollbar clicks/drags unexpectedly starting or pausing.
- Double-click inside the Reader toggles play/pause.
- Native scrollbars and pane splitters are excluded from double-click playback.
- Existing single-click text seek remains intact.
- Alt+double-click remains reserved for Ask Mark paragraph selection.
- Existing Start/Pause buttons remain unchanged.
- No /reader engine files modified.
