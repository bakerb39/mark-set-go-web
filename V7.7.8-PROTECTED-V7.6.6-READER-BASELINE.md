# Version 7.7.8 — Protected v7.6.6 Reader Baseline

This build starts from the exact locally verified v7.6.6 project supplied by the user.

## Protected core

The following reader files are copied unchanged from that baseline:

- `app.js`
- `public/app.js`
- `reader/`
- `public/reader/`
- `modules/`
- `public/modules/`

Authentication, the private beta gate, and PostgreSQL support were added around the reader through `auth.js`, server routes, database files, minimal navigation markup, and authentication-only CSS.

Do not alter protected reader files without an explicit reader requirement and regression testing.
