MARK, SET, GO! CHAT — SERVER.JS EDIT

Make TWO small edits to the existing server.js in bakerb39/mark-set-go-web.

1) Near the top, immediately after this existing line:

const { pool, checkDatabase, databaseConfigured, closeDatabase, query } = require('./db');

add:

const installMarkSetGoChat = require('./msg-chat-routes');


2) Near the very bottom, immediately BEFORE this existing catch-all route:

app.get('*', (_req, res) => res.sendFile(path.join(__dirname, 'public', 'index.html')));

add:

installMarkSetGoChat(app, { query, databaseConfigured });


That is all that should be changed in server.js.
Do not replace or alter the reader routes.
