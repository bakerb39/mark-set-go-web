# v5.42 Reading / Display Cleanup

- Removes **Two Columns** from all Reading Mode selectors.
- Any older saved session that contains `two-column` automatically falls back to Highlight.
- Expanded Reading and Display settings panels now use a subtle light-green treatment so users can quickly identify which panel is open.
- Fullscreen gets a darker green version to maintain readable contrast.
