MARK, SET, GO! CHAT — PUBLIC/INDEX.HTML EDIT

Make THREE small edits to the existing public/index.html.

1) In <head>, add this stylesheet with the other CSS files:

<link rel="stylesheet" href="/msg-chat.css?v=1.0.0">


2) In <head>, add this script with the other deferred scripts:

<script defer src="/msg-chat.js?v=1.0.0"></script>


3) In the main navigation, find the existing Profile button:

<button class="top-level-nav-button" data-action="profile-preferences" type="button"><span class="nav-icon" aria-hidden="true">◉</span> Profile</button>

Immediately BEFORE it add:

<button class="top-level-nav-button" data-action="msg-chat" type="button"><span class="nav-icon" aria-hidden="true">◫</span> Chat</button>


THEMES:
Your current GitHub index already has Profile as the theme/settings destination.
Do not remove theme controls from Profile.
If a separate top-level Themes button exists in a later version, remove only that top-level button/menu item.
