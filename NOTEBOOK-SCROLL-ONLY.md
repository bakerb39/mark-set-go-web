# Notebook scroll repair

Changes only the standalone My Notebook page scrolling:
- `.global-notebook-page` now owns vertical overflow when content exceeds the viewport.
- No reader, context-menu, right-click, companion, walkthrough, or application runtime code changed.
