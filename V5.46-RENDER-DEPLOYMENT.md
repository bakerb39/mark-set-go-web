# v5.46 Render Deployment Build

- Adds `render.yaml` for Blueprint deployment.
- Adds `/healthz` for Render health checks.
- Explicitly binds the Node server to `0.0.0.0`.
- Uses Render-provided `PORT`.
- Adds graceful SIGTERM/SIGINT shutdown.
- Keeps `OPENAI_API_KEY` server-side only.
- Adds model environment variables with `gpt-5` defaults.
- Adds `.gitignore`, expanded `.env.example`, and `RENDER-DEPLOYMENT.md`.
- Adds `npm run check`.
