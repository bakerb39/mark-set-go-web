{
  "name": "mark-set-go-web",
  "version": "5.46.0",
  "private": true,
  "description": "Unified public-library reader and reading accelerator with guided highlighting, imports, progress, music, and illustrated books.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/reader/VirtualRenderer.js && node --check public/reader/SessionManager.js"
  },
  "engines": {
    "node": ">=20 <23"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "express": "^4.21.2",
    "adm-zip": "^0.5.16"
  }
}
