# v7.0.7 — Reader bottom spacing

- Added a dedicated `reader-page-panel` class to the reader route.
- Removed the global `650px` app-shell minimum height on the reader page only.
- Removed the reader page shell bottom margin so the site footer follows the reader controls without a blank band.
- Other pages retain their existing minimum-height layout.
