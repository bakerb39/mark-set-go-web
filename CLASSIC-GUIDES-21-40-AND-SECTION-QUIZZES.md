# Classic Guides 21–40 + section quizzes

Built from the user-supplied current full ZIP.

- Adds Classic Guides 21–40 in Great Books order after Herodotus.
- Adds `[[MSG:SECTIONQUIZ]]` after each existing Classic Guide Discuss checkpoint.
- `Quiz me` uses the same `/api/comprehension` + `renderComprehensionQuiz` flow as the established whole-guide quiz, but passes only the preceding guide section.
- Existing `[[MSG:QUIZ]]` remains the whole-guide quiz.
- Approved bottom action toolbar CSS is unchanged.
- Root/public copies are mirrored.
- Chromium regression intentionally skipped for this request at the user’s direction.
