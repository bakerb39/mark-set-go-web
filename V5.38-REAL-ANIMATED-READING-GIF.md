# v5.38 Real Animated Reading GIF

- The previously supplied `.gif` asset was actually PNG image data with only one frame.
- Replaces it with a genuine multi-frame animated GIF.
- Uses subtle left/right eye-region motion to simulate rapid reading.
- Keeps the simplified v5.37 startup layout unchanged.
- Bumps the app/cache version so the browser requests the new asset.
